export class Statistic {
    constructor(
        // 索引 ID
        public id: string,
        // 组织名
        public organization: string,
        // 项目
        public project: string,
        // 访问次数
        // 被搜索率
        // 展示次数
    ) {
    }
}