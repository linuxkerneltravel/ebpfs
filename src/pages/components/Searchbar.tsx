import {Component} from "react";
import Avatar from "@/pages/components/Avatar";

export default class Navbar extends Component<{}, {}> {
    render() {
        return (
            <div className="glass fixed z-50 w-full shadow-lg flex items-center"></div>
        );
    }
}